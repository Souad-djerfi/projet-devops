version: '3'

volumes:
    app:
    database:
    redis:

services:
    flask-app:
<<<<<<< HEAD
       # container_name: flask-app
        #build: ./python/
        #command: sh -c "sleep 10s; python3 ./app.py"
        image: souaddjerfi/flask-app
=======
        container_name: flask-app
        #build: ./python/
        #command: sh -c "sleep 10s; python3 ./app.py"
        image: souaddjerfi/flask-app:latest
>>>>>>> fa5f11a43d0b5a20f31dc0d18a927d403c64fba0
        restart: always
        ports:
            - '5000:5000'
        volumes:
            - app:/app
        depends_on:
           - mysql-db
           - redis-db    
        
    mysql-db:
        container_name: mysql-db
        #build: ./database/
        image: souaddjerfi/mysql-db
        restart: always
        command: --default-authentication-plugin=mysql_native_password
        environment:
            MYSQL_ROOT_PASSWORD: root
            MYSQL_DATABASE: test
        ports:
            - '3306:3306'
        volumes:
            - database:/var/lib/mysql
        
    redis-db:
        container_name: redis-db
        image: redis:latest
        restart: always
        ports:
            - '6379:6379'
        

              






