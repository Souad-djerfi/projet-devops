- name: playbook deployer decoker-compose
  become: yes
  hosts: all
  tasks:
    - name: démarrer Docker
      systemd:
        name: docker
        state: started
        enabled: yes 

    - name: stopper docker-compose s'il tourne
      docker-compose:
        project_src: /tmp/
        state: absent
        ignore_errores: yes


    - name: supprimer les conteneurs 
      docker_container:
        name: falsk-app, mysql-db, redis
        state: absent

          
    - name: lancer docker-compose 
      docker_compose:
        project_src: /tmp/
        state: present 
        #scale: 
          #flask-app: 2
    
